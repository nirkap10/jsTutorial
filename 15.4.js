function calcAverage (arr){
    var sum = 0 ;
    for ( var i = 0 ; i < arr .length; i ++ ){
    sum += arr [ i ];
    } return sum/3 ;
    }
    calcAverage ([ 85 , 90 , 92 ]);
    //1 2 5
    //2 vs debugging
    //3 trying to add to a var without value and not dividing the result